---
description: List this session's active PR monitors
---

Call the pr_monitor tool with action "status" and relay the result verbatim. If there are active monitors, remind me I can stop one with the pr_monitor tool (action "stop") or get an immediate full report (action "flush").
